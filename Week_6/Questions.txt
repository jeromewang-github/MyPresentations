1.我觉得NSPCA中间缺乏一种机制来保证存在正的驻点，否则也会迭代到0
2.修改后的目标函数形式是严格的二次多项式（无常数项）且过原点，由于系数的正负性不确定，导致目标函数极可能是凸的，也可能是凹的。就连极值点所在的象限都无法确定。
3.我觉得NSPCA跟我们的很相似，只不过目标函数不一致，我们的目标函数目前是二次的，他的是四次的。求解方法也就是一个求极值找最值的问题。如果我们能保证目标函数是凸的，且最小值在第四象限，那么这时候也既可以借鉴NSPCA，也可以用乘法迭代来求解的。但是乘法迭代的非负性要依赖于输入数据，但是NSPCA的求解过程非负性的保持不需要依赖于输入数据，这是一个亮点。